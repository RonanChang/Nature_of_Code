function setup() {
  createCanvas(500, 500);
  background(0);
}

function draw() {
  if (mouseIsPressed) {
    background(random(255), random(255), random(255));

  }
}

function mousePressed() {

  //background(random(255),random(255),random(255));

}