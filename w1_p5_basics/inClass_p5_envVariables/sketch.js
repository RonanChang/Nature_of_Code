var x;
var y;

function setup() {

  createCanvas(500, 600);

  x = width / 2;
  y = height / 2;//this can not be put before createCanvas
  background(100);

  ellipse(x, y, 200, 200);
}

function draw() {

}